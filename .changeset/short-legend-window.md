---
"@kud/gh-ink": patch
---

Legend no longer gets topped on a short terminal. It now shows a window of itself with the column heading reprinted above the slice, `↑↓` / page keys to move through it, and a `n–m of total` counter; the tab-count footnote gives up its rows while scrolling.
