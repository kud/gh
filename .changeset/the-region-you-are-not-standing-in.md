---
"@kud/gh-ink": minor
---

The rail is twelve columns wider, and the list recedes while the rail holds the arrows.

Two complaints about the same panel. At 40 columns the label line had 33 to spend, which is under half a real initiative title — the rail listed keys with an ellipsis after them, and a key you cannot read a title against is a key you have to open something else to place. `SIDEBAR_COLS` is 52.

The second is that focus was invisible. The keymap has always shut the list out the moment `⇥` hands the rail the arrows, so every row binding below that branch was already inert — but both cursors stayed lit, and nothing on screen said which one `↵` would act on. The list now takes the same backdrop an overlay gives it: one vocabulary for "the region you are not standing in", not two. The row you will come back to keeps its `❯`, in the backdrop's tone.

The rail's width is the lever the row-fitting specs pull, so their rungs moved with it — `narrow.test.tsx` and `labels.test.tsx` are recalibrated, and the light-pressure fixture is depth 0 so the ladder is measured in the columns the spec means rather than in a tree prefix.
